# Secrets — real config never committed
config.py

# Trade + scanner logs
fills.jsonl
triggers.jsonl

# Runtime dashboard heartbeat
status.json
status.json.tmp

# Learned IBKR compliance blocklist (account-specific runtime state)
blocklist.json

# Retrospective: decision log, generated reports, learned playbook
# (playbook.md is curated knowledge — `git add -f playbook.md` if you want it versioned)
decisions.jsonl
reports/
playbook.md
playbook_pending.md

# Generated charts
charts/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.log
